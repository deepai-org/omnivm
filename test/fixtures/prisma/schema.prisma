generator client {
  provider = "prisma-client-js"
  output   = "./generated/client"
}

datasource db {
  provider = "postgresql"
}

model Order {
  id     Int    @id @default(autoincrement())
  name   String
  items  String
  keys   String
  count  Int
  then   String
  length Int
  close  String
}
