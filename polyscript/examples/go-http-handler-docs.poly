// Docs-style Go package usage: net/http HandlerFunc and request construction.

import http from "net/http"

func docs_handler() {
  handler := http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
    w.Header().Set("x-poly", "yes")
    w.WriteHeader(http.StatusAccepted)
    _, _ = w.Write([]byte(r.URL.Path))
  })
  return fmt.Sprintf("%T:%d", handler, http.StatusAccepted)
}

const response = docs_handler()

print(f"Go HTTP HandlerFunc docs {response}")
